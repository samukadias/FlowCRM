# Banco separado do de desenvolvimento — os testes truncam as tabelas a
# cada execução, então nunca deve apontar para o mesmo banco do `npm run dev`.
DATABASE_URL="postgresql://propostaflow:propostaflow@localhost:5434/propostaflow_test"
AUTH_SECRET="segredo-de-teste-nao-usado-em-producao"
